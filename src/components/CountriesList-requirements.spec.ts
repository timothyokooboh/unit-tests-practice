import { describe, it } from 'vitest'

describe('CountriesList', () => {
  describe('before data has loaded', () => {
    it('renders a loading UI', () => {})
  })

  describe('when data has loaded', () => {
    it('renders a view containing the list of countries', () => {})

    it('renders the names of countries and their flags', () => {})
  })
})
